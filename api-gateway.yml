management:
  endpoints:
    web:
      exposure:
        include: health, metrics, env
  endpoint:
    health:
      show-details: always
    env:
      show-values: always
eureka:
  client:
    service-url:
      default-zone:
http://localhost:8761/eureka
